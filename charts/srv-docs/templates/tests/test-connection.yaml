apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "srv-docs.fullname" . }}-test-connection"
  labels:
    {{- include "srv-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "srv-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
