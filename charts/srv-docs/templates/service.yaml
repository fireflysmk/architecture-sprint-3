apiVersion: v1
kind: Service
metadata:
  name: {{ include "srv-docs.fullname" . }}
  labels:
    {{- include "srv-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "srv-docs.selectorLabels" . | nindent 4 }}
