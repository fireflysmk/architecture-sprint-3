apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "srv-telemetry.fullname" . }}-test-connection"
  labels:
    {{- include "srv-telemetry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "srv-telemetry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
