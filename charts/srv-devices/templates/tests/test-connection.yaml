apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "srv-devices.fullname" . }}-test-connection"
  labels:
    {{- include "srv-devices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "srv-devices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
